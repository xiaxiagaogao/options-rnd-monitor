# 研究助手 — 需求框架 v0

> 状态：**框架已决项**（2026-07-20）。本文档是「研究助手」专题的工作 SSOT 草案。  
> 与主 SSOT `rnd-dashboard-spec.md` 冲突时：主 spec 管管线/指标/纪律；**本专题的产品形态与调用边界以本文为准**，决议回写主 spec 前两文档并存。  
> 范围：立法与边界。**不含实现排期细项、不含模型供应商最终选型。**

---

## 0. 一句话

在 **EOD 世界地图**之上，提供一个 **只读、数值钉死、单次生成** 的研究助手：  
- **交互面**：独立页面问答（侧栏入口）；  
- **推送面**：一键报告经外部通道送达（首版只做一种通道）。

输出性质：**数据描述，不构成投资建议**。RN ≠ 真实概率。

---

## 1. 与既有产品的关系

| 项 | 裁决 |
|---|---|
| 产品阶段 | **V1+**（主线 §8 第 0–6 步已完成之后） |
| 主产品 | 仍是 RND 仪表盘（罗盘 / 雷达 / 温度计 / 底座） |
| 助手角色 | **解读层**：组织语言、套纪律与白话；**不算数、不改库、不下单** |
| 远期（非本 v0） | 对接 agent 交易平台 **intent schema**（spec §0 方向句；字段未定义） |
| 明确非目标 | 盘中 co-pilot、GEX/OI 叙事、模型自算 RND、自主 tool-calling agent |

### 1.1 继承的硬纪律（来自主 spec §6 / UI 铁律，助手必须服从）

1. 冻结线写入 journal 后不可变；偏移量是加减仓信息，不是重画失效线的借口。  
2. 分位线只做**收盘确认**的论点失效线；禁止当盘中硬止损。  
3. 仓位反解与 identity（投机 Q25 / 信念 Q05）逻辑不变。  
4. roll 预定义重钉；禁止用 roll 挪失效线。  
5. 目标 > Q95 须书面理由（journal 既有字段）。  
6. RN ≠ 真实；中心钉远期；不含方向观点；异常须 252 日分位 + 指数并排。  
7. 闸门是质量验收，**不是交易信号**；FAIL → 说明静默/置灰原因，不编信号。  
8. 样本 < 60 → 不得陈述假精确分位。  
9. 可信度排序：Q25–Q75 > Q05/Q95 > 偏度 > 峰度。

---

## 2. 产品形态（已决）

### 2.1 入口与信息架构

| 项 | 已决 |
|---|---|
| 主导航 | **侧栏** |
| 卡片位置 | **「研究助手」置于「事件流」之上**（当前实现在添加标的之后，落地时调整顺序） |
| 路由 | **独立 URL 页面**（例：`/assistant`；最终 path 实现时定） |
| 与仪表盘关系 | 并列二级面；仪表盘可深链带入上下文（`symbol` / `date` query，实现时定） |
| 卡片既有文案（保留精神） | 只读本地五表 · 输出为数据描述非建议 |

侧栏结构目标序（驾驶舱区）：

```text
标的卡片列表（overview）
研究助手          ← 入口（链到独立页）
事件流
添加标的 / 动态槽
```

### 2.2 双能力（已决）

| 能力 | 形态 | 说明 |
|---|---|---|
| **A. 问答** | 独立页内 | 用户自然语言提问；绑定当前上下文（标的 / 数据日等） |
| **B. 一键报告** | **推送**送达 | 生成固定结构的「收盘解读」；经外部通道推送，而非仅页内长文阅读（页内是否同时展示：实现可选，非阻塞） |

### 2.3 一键报告推送通道（已决原则，首版收窄）

| 项 | 已决 |
|---|---|
| 通道候选（暂定名单） | Telegram Bot、邮箱 |
| 首版 | **只实现一种**（实现前再选；另一种留接口位） |
| 触发 | 按需一键（用户触发）；定时 cron 推送 **非 v0 必做**（可列为 v0.1） |
| 内容性质 | 与问答同一套数据装配 + 同一套输出纪律；报告更偏「全景模板」 |

### 2.4 页内上下文（问答）

| 项 | 已决 / 默认 |
|---|---|
| 必选上下文 | `symbol`、数据日 `asof`（默认该标的最新 pinned 日） |
| 隐含上下文 | pinned 到期、闸门、持仓（若有 open journal） |
| 切换 | 助手页内可改标的/日期；不依赖必须停留在仪表盘主视图 |
| 多轮 | 允许；**每一轮 = 一次新的单次请求**（见 §4），服务端按该轮上下文重装配 |

---

## 3. 数据接触方式（已决）

### 3.1 硬约束

> **LLM 只消费服务端装配好的结构化数据，不另外计算。**

| 允许 | 禁止 |
|---|---|
| 引用装配包中的字段、单位、已算分位与 Δ | 心算/重算 IV、密度、分位、σ、PIT |
| 组织语言、排序、对照、套白话与纪律 | 改写数值、 invent 未提供字段 |
| 在闸门 FAIL 时解释 `gate_detail` 已有项 | 声称「仍可交易信号」或绕过静默规则 |
| 声明样本不足 / RN 口径限制 | 把 RN 说成真实概率；盘中止损话术 |
| — | 读 `raw_chain` 逐行权价自行拟合 |
| — | 默认吞入整段 `rnd_curve` 网格做二次推导 |

### 3.2 数值唯一来源

```text
五表（只读）
  → 确定性查询 / 上下文装配器（扩展现有 server/queries 精神）
  → 结构化上下文包（JSON 或等价表）
  → 系统提示（纪律 + 模板）+ 用户问题
  → LLM
  → 文本（及可选结构化大纲字段，若实现需要）
```

- **五表构成**：`raw_chain / rnd_curve / rnd_indicators / rnd_state / trade_journal`（第 5 张为持仓日志，助手**只读不写**）。注意 `fit_meta` 是 `rnd_curve` 内的 TEXT 列、**不是独立表**。  
- 主数据语义：库内 **ThetaData EOD 管线产物**。  
- Polygon 等辅源：**不**经助手路径参与 RND 数值；若将来进叙事，须单独标记非 RND 口径（本 v0 可不做）。

### 3.3 上下文包最小集（v0 需求级，字段名实现可调整）

装配器必须能提供（有则填、无则显式 null + 原因）：

| 块 | 内容 |
|---|---|
| 元信息 | symbol、asof、pinned expiry、dte、数据新鲜度声明 |
| 定位类 | F、±1σ、Q05–Q95、mode、**逐分位报价区标志** `q05_in_range … q95_in_range`（非单一布尔；短 DTE 常 Q25/Q75 在区内、Q05/Q95 落外推区，粒度支撑 §1.1-9 可信度排序） |
| 状态类 | 10 个 STATE_INDICATORS 的 value / pct / dpct / sample_n + 既有 SAYINGS |
| 质量 | gate_pass、gate_detail、n_modes / peaks 摘要 |
| 事件 | 近窗 roll / 闸门 / 双峰等（与仪表盘事件同源口径） |
| 持仓 | 若有 open：identity、stop_q、冻结线、相对现价/现 Q 的偏移（服务端算好） |
| 对照 | 指数基准并排状态（沿用现有 benchmark 规则精神） |
| 可选摘要 | density/PIT 的**服务端标量摘要**（非全网格）；闸门 FAIL 时 diagnostics 摘要 |

**全网格曲线、原始链：默认不进 prompt。**

### 3.4 输出模板（继承已有规划）

采用 `docs/hyai-design-reference.md` §2 精神，至少：

- 标题带**数据口径日期**  
- **维度 \| 读数 \| 白话**（读数必须来自上下文包）  
- 需要时：**公式 +「翻译：」**（公式只解释已给数，不引入新算）  
- **纪律引用块**（历史统计·非预测·RN≠真实 等）  
- **样本量 / 闸门 / 外推区**披露脚注（外推区须**点名具体落区分位**：如"Q25/Q75 报价区内可信，Q05/Q95 落外推区、按 in_range=False 兜底"，而非笼统"有外推"）  
- 问答结尾可给「还可以看」类追问建议（不自动发起）

---

## 4. 模型与调用链（已决）

| 项 | 已决 |
|---|---|
| V1 调用形态 | **单次请求（single-shot）** |
| 多轮定义 | 多次单次；每轮服务端**重新装配**上下文 + 一次 LLM |
| Agent 链式 tool-calling | **非 V1**；推迟，解锁条件另议 |
| 模型与配额 | 仍为接入阻塞项（现 `POST /api/assistant` → 501 原文）；选型本框架不锁厂商 |
| 写库 | 默认不写业务表；若落「生成日志」另开需求（非 v0 必做） |

```text
V1 合法调用链

  [装配上下文] --1次--> [LLM 生成] --1次--> [返回 / 推送]

非法（V1）

  LLM → 自主选 tool → 再算 → 再问 …（agent 环）
  LLM → 数值计算
```

---

## 5. 能力边界与拒答

助手在以下情况应**降级或拒答对应部分**（语气仍可礼貌完整）：

| 情况 | 行为 |
|---|---|
| 闸门 FAIL | 只解释质量问题与诊断摘要；状态分位按静默规则处理 |
| sample_n < 60 或 pct 为 null | 不报分位排名 |
| 无 OI / 未实现指标（GEX、VRP 等） | 明确「本系统无此数据」 |
| 要求盘中硬止损 / 下单 | 引用纪律拒答 |
| 要求预测涨跌方向 | 拒答方向观点；可描述 RN 分布与分位状态 |
| 上下文包缺字段 | 显式说缺，不编造 |

---

## 6. 与现有代码锚点（现状，非实现任务）

| 锚点 | 现状 |
|---|---|
| `POST /api/assistant` | 501 桩；注释指向 hyai 模板与「模型与配额」 |
| `web/index.html` 研究助手卡片 | 接入预留；位于添加标的之后（**目标改到事件流之上**） |
| `server/queries.py` | 确定性读数 + SAYINGS/LABELS；装配器应复用此精神 |
| 主 spec | 无助手专章；代码悬空引用 `spec §8.5`（`server/api.py` 头 + 第 148 行）与 `§8.6`（`rnd/admission.py`），而 spec §8 实为 0–6 平铺列表、无此二子节——**助手立法以本文为准**，回写主 spec 时补 §8.5 / §8.6 两处指针 |

---

## 7. 非目标（本框架明确砍掉）

- 真·日内实时助手、希腊盘中面板依赖  
- 模型读取原始链 / 全曲线自由重算  
- V1 多步 agent  
- 首版同时上线 TG + 邮箱双通道  
- 商业化 token 计费 UI  
- 自动交易 / 改 journal 冻结线  

---

## 8. 已决清单（速查）

1. **入口**：侧栏；研究助手在**事件流上方**；点进**独立 URL 页**。  
2. **能力**：页内**问答** + **一键报告推送**（TG / 邮箱候选，**首版一种**）。  
3. **数据**：LLM **只拿装配数据，不另外计算**；数值钉死在查询层。  
4. **调用**：**V1 = 单次请求**；多轮 = 多次单次 + 每轮重装配。  
5. **性质**：只读 · 数据描述非建议 · 服从 spec §6 纪律。

---

## 9. 待决（不阻塞框架，阻塞实现前需定）

| # | 项 | 备注 |
|---|---|---|
| T1 | 模型供应商 / 型号 / 密钥位置 | 501 阻塞项 |
| T2 | 首版推送通道二选一：TG Bot vs 邮箱 | 暂定名单 |
| T3 | 最终 path 名、鉴权是否与仪表盘同一 cookie | 倾向同一会话 |
| T4 | 一键报告是否同时在页内渲染一份 | 可选 |
| T5 | 生成日志是否落库 | 默认否 |
| T6 | 回写 `rnd-dashboard-spec.md`：补 §8.5（仪表盘/助手）、§8.6（准入检查器）指针，消解代码悬空引用 | 文档同步 |

---

## 10. 修订记录

| 日期 | 变更 |
|---|---|
| 2026-07-20 | v0：形态（侧栏位置 + 独立页 + 问答 + 推送报告）、数据钉死、V1 单次请求 立法 |
| 2026-07-20 | v0.1：口径对齐实现——逐分位报价区标志（非单布尔）、五表构成 + fit_meta 为列非表、悬空引用补 §8.6 |
