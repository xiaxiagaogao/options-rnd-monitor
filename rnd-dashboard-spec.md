# RND 仪表盘 — 设计交接文档

> 来源：claude.ai 设计会话，2026-07-12。本文档是设计阶段全部已锁定决策的单一事实来源（SSOT）。
> 实现前先通读。与代码冲突时以本文档为准，除非明确推翻某条并同步更新本文档。

## 0. 一句话定位

从美股期权链提取风险中性密度（RND）的 EOD 管线 + 可视化仪表盘。两个用途：主观交易的分位数失效线 / 止盈参考（当下），量化信号层特征（将来，对接 agent 交易平台的 intent schema）。

**V0 原则：按"能出信号的系统"打地基（历史快照第一天起落库），但 V0 只交付可视化。** RND 指标的价值大部分在时序变化（分位数状态），不在单日水平——所以存储层是核心不是附属。

## 1. 已锁定决策

| 决策项 | 内容 | 关键理由 |
|---|---|---|
| 主数据源 | ThetaData 免费档 | 免费含 1 年历史 EOD 期权数据 → 分位数指标上线首日即有效（冷启动消失）；20 req/min 对 EOD 场景足够 |
| 辅数据源 | Polygon（改名 Massive 中）免费 key | 仅作合约参考数据 / 标的收盘价的交叉校验与兜底。其免费档是成交口径的日聚合，翼部无成交时陈旧或缺失，**禁止用于 RND 计算** |
| IV | 一律自算 | 从收盘 bid/ask 的 mid 反解；不信任何 vendor 的 IV 字段 |
| 标的池 | QQQ / SPY / NVDA 固定 + 2 个动态槽 | 落配置文件（symbols.yaml），不写死代码 |
| 更新频率 | EOD | 用 vendor 收盘报告（规范的 close bid/ask，避开盘后报价陈旧变宽的坑）；cron 定在美股收盘后，东京时间约 07:00 |
| 到期日 | 仅月度期权；最近两个月度（DTE 限 7–60）各算一套入库 | 周度期权翼部太薄毁尾部。指标时序只钉"最接近 30 DTE"那套并记录 DTE；roll 日打标（不钉住会在换月日产生锯齿假信号） |
| 存储 | SQLite，五表（原始三表 2026-07-14 扩为 +rnd_state +trade_journal） | 体量小（见 §3）；原始层与日志层不可变 |
| 利率源 | ThetaData `interest_rate_history_eod`（SOFR）；FRED 备胎 | 免费档实测可用，同源省一个外部依赖。实测利率端点历史仅到 2024-01，更早的回填段用 FRED CSV（免 key） |
| 付费升级触发 | 日内 RND 出现明确用途时（如财报日双峰迁移盯盘），**不是**"有盈利"就升级 | 盈利但 EOD 够用 = 不花这个钱。升级前实测报价端点在哪个档位（Polygon 期权 NBBO 历来锁高档） |

### 免费档实测结论（2026-07-13 回填）

- **接入方式**：官方 Python 库 `thetadata` 1.0.9 直连（HTTPS 鉴权 + gRPC 数据，API key），**无需 ThetaTerminal.jar**。要求 Python ≥3.12。
- **EOD 期权报告字段**：`symbol, expiration, strike, right, created, last_trade, open, high, low, close, volume, count, bid_size, bid_exchange, bid, bid_condition, ask_size, ask_exchange, ask, ask_condition`——close bid/ask 齐全，零成交行权价也有双边报价；`created` 时间戳约 17:20 ET（EOD 报告生成时刻）。一次请求可拉整链（`strike='*', right='both'`）。
- **OI 端点**：免费档 `PERMISSION_DENIED`，明确关闭。`raw_chain.oi` 置 NULL，升级付费档后回补。
- **历史深度**：实测 ≈ 3 年滚动（2023-07-14 可用，2022-07-08 拒绝），远超宣称的 1 年。回填空间足够铺 3 年。
- **限速**：轻请求 25 连发未触发限流；整链请求单次延迟 11–14 s；批量拉取时服务端渐进限流，单请求可劣化至 5–10 分钟（3 年 × 3 标的全量回填实测约 12 小时）。
- **范围请求上限**：股票 EOD 范围请求单次最多 365 天（超出报 INVALID_ARGUMENT），需分块；期权按到期拉不受影响；利率端点无此限制。
- **利率**：`interest_rate_history_eod('SOFR')` 免费可用，日频。
- **网络注意**：Clash TUN（fake-IP）环境下 gRPC 直连握手失败，须设 `grpc_proxy=http://127.0.0.1:7897`（本机混合端口）；VPS 无代理环境预期直连，部署时验证。
- **Polygon/Massive 交叉校验**：v3 reference contracts、v2 daily aggs 均可用；SPY 2026-07-06 收盘 751.28 与 ThetaData 一致。

## 2. 动态槽准入规则

- **链质量检查（换入前强制、自动执行）**：ATM 邻域（收盘 ±5%）相对点差中位数 ≤ **10%**；±20% moneyness 范围内双边报价的行权价 ≥ **15** 个。不达标拒绝入池。
  - 阈值校准记录（2026-07-17，初版 5%/20）：在池实测 SPY 1.3%/176、QQQ 0.9%/57、NVDA 1.2%/**16**——初版"≥20"会误杀 NVDA 级健康链（高价股行权价间距宽）；候选实测 AMD 2.4%/20 PASS（与原人工判断一致）、RKLB 4.1%/6 FAIL（同）——但 RKLB 点差能过初版 5% 线，故点差收紧至 3%。实现：`rnd/admission.py`。
  - 二次校准（2026-07-27，持仓同步上线）：点差 3% → **10%**。币安代币化美股永续的底层期权流动性普遍弱于主流指数/NVDA；同日 EOD 同源实测在池 SPY/QQQ/NVDA 仍 1.1–1.7%（证明非数据源/时点问题），而持仓候选 MU 3.4% / INTC 5.2% / SNDK 5.2% / GOOGL 5.8% 与 HOOD 10.8% / RKLB 15.3% 之间有天然缺口——10% 落缺口内，纳入流动性尚可的持仓标的、仍挡住 HOOD/RKLB 级薄链。放宽准入的安全性由**每日无套利闸门**（`checks.no_arbitrage_checks`，针对 RND 结果的另一套指标）独立兜底：点差宽的链若违反无套利则 `gate_pass=0`、state 分位置 NULL，只呈现"质量未过"而非假信号。coverage 仍 15（NOK/FLNC 双边仅 6，行权价太少被挡）。
- **换入即回填**：用 ThetaData 的 1 年历史把新标的指标序列补齐，换入当天分位数即有效。
- **换出不删数据**，历史无害。
- 已知排除：RKLB 链太薄，不进池。AMD / MU 够格但仅用月度到期。

## 3. Schema（SQLite）

核心原则：**原始层不可变（append-only），派生层可全量重算。** 平滑算法必然迭代，原始报价在手则全部历史指标可重算。**脏数据照存，过滤发生在计算时而非入库时**——过滤规则本身也要可迭代。

```
raw_chain(date, symbol, expiry, strike, right, bid, ask, volume,
          oi NULLABLE, quality_flags, underlying_close, sofr)
  -- quality_flags: one_sided / crossed / wide_spread（入库打标，不剔除）
  -- oi: 免费档 PERMISSION_DENIED（2026-07-13 实测），置 NULL，升级后回补

rnd_curve(date, symbol, expiry, grid_json, fit_meta)
  -- grid_json: {strikes[], density[]}，存曲线本身，仪表盘回看历史免重算
  -- fit_meta: 平滑参数、有效行权价数、拟合误差、外推方法与边界

rnd_indicators(date, symbol, expiry, <每指标一列标量>)
  -- 各分位数附 in_quoted_range 布尔：落在真实报价范围内 vs 外推区

rnd_state(date, symbol, indicator, value, pct, dpct, sample_n)  -- 第 4 表，2026-07-14 增
  -- 状态层（§4 通则的落地）：pinned 序列上状态类指标的 252 日滚动分位 + 日环比 Δ
  -- tidy 长表（指标集迭代免迁移）；纯派生可全量重算（scripts/build_state.py）
  -- pct: 滚动分位[0,100]；样本<60 或当日闸门失败 → NULL（防假精确/信号静默）
  -- dpct: 日环比分位 Δ；roll 日 → NULL（避免换月锯齿）

trade_journal(event_id, position_id, event_type, event_date, symbol,   -- 第 5 表，2026-07-14 增
              direction, identity, entry_price,
              frozen_expiry, frozen_q05, frozen_q25, frozen_q50, frozen_q75, frozen_q95,
              frozen_sigma1, frozen_forward,
              stop_q, risk_budget, size,
              target_price, target_rationale,
              close_price, close_reason, notes)
  -- 交易日志（§6 纪律的载体，罗盘层数据源；实现排期在第 5 步仪表盘）
  -- append-only 事件流，同原始层待遇：event_type = open / roll_repin / close，
  --   同一持仓的事件用 position_id 串联，"修改"一律表达为追加新事件
  -- 冻结线不可变的机制保证：open/roll_repin 事件写入当日全套分位快照后永不 UPDATE，
  --   反处置效应的预承诺装置；实时带与冻结线的偏移量由视图层现算
  -- identity: speculative（钉 Q25）/ conviction（钉 Q05）；size = 风险预算 ÷ |入场价 − 冻结 stop_q 线|
  --   反解由录入界面代算，结果小到没意思 = 该身份不成立（§6）
  -- roll_repin: 跨到期持仓在 roll 日重钉下一月度对应分位（§6 预定义规则的执行痕迹）
  -- target_price > frozen_q95 时 target_rationale 必填（"下市场赔率 <5% 的注，凭什么我对"）
```

体量参考：5 标的 × 2 到期 × 数百行权价 ≈ 每日数千行；十年量级几百 MB。

## 4. 指标集

**通则：水平类直接用；状态类必须经 252 日滚动分位 + 日环比 Δ 变换后才允许进策略与仪表盘信号区；样本 < 60 天不显示分位（防假精确）。**

| 层 | 指标 | 说明 |
|---|---|---|
| 定位类（价格位输出） | 隐含远期 F | put-call parity 从链上反推，顺带绕开股息估计 |
| | ±1σ 预期波动 | 同时给 ±$ 和 ±%；策略用途 = 噪音锥检查（止损距离 < 1σ 视为送钱） |
| | Q05 / Q25 / Q50 / Q75 / Q95 | 分位数价格位，失效线与止盈参考的原料 |
| | 众数 | 密度峰值位，与 F 的偏离方向是另一视角的倾斜 |
| 状态类（须分位变换） | 偏度 | 个股左尾突肥 = 对冲需求升温；须配合指数对照 |
| | 超额峰度 | 对翼部数据最敏感、四个矩里最脆，必须挂质量闸门 |
| | ATM IV | 等价 IV rank；高分位防 vol crush，低分位适合埋伏 |
| | 25Δ 风险反转 | 独立口径情绪计，与偏度互为校验 |
| | 分位数偏度（Bowley） | `[(Q95−Q50)−(Q50−Q05)]/(Q95−Q05)`；矩偏度的稳健口径对照——分位数走 CDF 比三阶矩稳，分辨"真变肥"vs 外推抖动（2026-07-13 增补） |
| | 25Δ 蝶式（BF25） | `(σ25c+σ25p)/2 − σATM`；smile 曲率 = 峰度的独立口径校验，补齐"峰度无校验搭档"的缺口（2026-07-13 增补） |
| | 期限结构斜率 | 近月 ATM IV − 次月 ATM IV；倒挂分位 = 事件压力计 |
| | 尾部概率 | 密度直接积分；指数看 P(±5%)，个股看 P(±10%) |
| 事件类 | 双峰检测 | 平滑密度上数局部极大值；触发时输出两峰位置 + 各自概率质量（财报二元定价探测器） |
| 校验类（闸门非信号） | RR–偏度同向性 + 拟合误差 + 有效行权价数 | 任一不达标 → 该标的当日全部状态类信号静默，仪表盘置灰。宁可没信号，不可有假信号 |

> 校验口径修正（2026-07-14，回填数据实测）：RR–偏度同向性中的"偏度"必须用**对数空间偏度**（log(S_T) 的三阶矩，随 log_skew 列入库）。价格空间矩偏度自带对数正态正基线（平坦 smile 即正偏），高波动个股会被基线淹没——NVDA 3 年回填中 519 天出现"RR 为负、价格偏度为正"的假冲突，实为口径错配而非数据质量问题。价格空间偏度与 Bowley 偏度仍作为状态类指标保留（它们衡量的是价格分布本身的不对称，含基线是其定义的一部分）。

可信度排序（影响闸门分级与 UI 呈现）：**Q25–Q75 > Q05/Q95 > 偏度 > 峰度**。分位数走 CDF（一阶导），比密度（二阶导）稳一个量级。

**状态层实现口径（2026-07-14，第 4 步）**：滚动窗口 = trailing 252 个 pinned 交易日；参考样本 = 窗口内闸门通过且值非空的子集（闸门失败日不污染分布，其自身分位置 NULL = 信号静默）；样本 < 60 → NULL；分位用 percentileofscore(kind='mean')；roll 日保留在参考窗口内（pinning 保持 ~30DTE 可比）但日环比 Δ 置 NULL（跨到期不连续）。实测冷启动消失点 ≈ 回填起点后 60 交易日（2023-07-11 → 2023-10-03），与"1 年历史上线即有效"承诺一致。

V1 推迟项（原始层在，随时补算）：恒定 30DTE 期限插值、隐含 vs 实现波动的 VRP 追踪、财报 IV 分离（需财报日历）、日间分布位移距离。跨标的差值（个股−指数偏度）在视图层现算，不入库。

## 5. 算法核心管线（下一个工作项，坑最多，逐步实现逐步验证）

1. **清洗**（计算时过滤，非入库时）：剔除 crossed；剔除单边报价；相对点差超阈值剔除或降权；**仅用 OTM call + OTM put 在 ATM 拼接**（缓解美式期权偏差），一律用 mid。
2. **F 反推**：同到期 put-call parity（ATM 附近最活跃的若干行权价对取回归或中值）。
3. **mid → IV**：BS 反解（对 F 计价；横轴用 log-moneyness 或 delta）。
4. **IV 空间平滑**：初版 cubic smoothing spline；平滑参数写入 fit_meta。
5. **回价格空间**：BS 正算得平滑 C(K)，细网格。
6. **B-L**：数值二阶导 × e^(rT) → density；**一阶导 → CDF → 分位数反查**，逐分位打 in_quoted_range 标志。
7. **无套利与质量检查（即验收标准）**：C(K) 对 K 单调递减且凸；density ≥ 0；∫density ≈ 1（记录容差）；尾部外推策略显式声明并入 fit_meta；RR 与偏度同向。
   - 检查分区口径（2026-07-13 实现时确立）：**闸门以报价区内为准**（信号指标全部承载于此）；外推区结果全量记录进 fit_meta 但不进闸门——短 DTE 陡翼的线性总方差延伸可违反 Gatheral g≥0，是无报价区的数学事实而非拟合缺陷，Q05/Q95 落入时由 in_quoted_range=False 打标兜底。V1 升级路径：对数正态尾部嫁接。

已知坑清单（实现时逐条对照）：

- 禁止在价格空间直接平滑——数值二阶导放大噪声，必须在 IV 空间做。
- 不信 vendor IV；不用 ITM 报价；不忘贴现因子 e^(rT)。
- 翼部无报价区的外推必须显式（方法 + 边界入 fit_meta）；Q05/Q95 落入外推区必须打标。
- 平滑过度 → 双峰被抹平（事件类指标失效）；平滑不足 → density 出负值。平滑参数是核心超参，全程记录以便重算。
- 报价必须同一时间戳口径（EOD 报告天然满足；将来换源需自查同步性）。

## 6. 使用纪律（同时是产品设计约束）

- **冻结线 vs 实时带同图呈现**：入场日分位数写入交易日志后不可变（反处置效应的预承诺装置）；实时分位带另画。冻结线与实时带的偏移量 = 加减仓的信息，不是重画失效线的借口。
- **分位线只做收盘确认的论点失效线，禁止当盘中硬止损**——盘中触碰概率约为终值概率的 2 倍（反射原理量级），硬止损照旧放价格行为结构位。
- **仓位反解**：size = 单笔风险预算 ÷ (入场价 − 冻结 Q 线)。投机仓钉 Q25，信念仓钉 Q05。反解结果小到没意思 = 该身份不成立，降级或放弃——公式强制身份与仓位自洽。
- **roll 规则预定义**：跨到期持仓在 roll 日重钉下一月度的对应分位；不预定义，roll 会变成挪失效线的后门。
- **目标 > Q95 需书面理由**（"我在下市场赔率 <5% 的注，凭什么我对"）。
- **解读铁律**：RN ≠ 真实概率（尾部含保费、系统性偏肥，当尺子准当水晶球偏）；分布中心钉在远期，图不含方向观点；左偏是股票期权常态（smirk），"异常"必须由 252 日分位判定，且需同日指数图并排区分个股恐惧 vs 系统恐惧。

## 7. 技术栈与部署

FastAPI + SQLite + Vue3（与 GEO dashboard 结构同构）；APScheduler 或 cron 跑 EOD 任务；新加坡 VPS；数据接入用 ThetaData 官方 Python 库直连（gRPC + API key，Python ≥3.12，**免 Java sidecar**——2026-07-13 实测推翻原 ThetaTerminal.jar 方案，jar 保留为备选路径）；Polygon 免费 key 备用。

## 8. 建议推进顺序

0. 注册 ThetaData + Polygon 免费 key；实测免费档端点权限（EOD 报告字段、OI、真实限速）→ 回填 §1。
1. **Walking skeleton**（单标的单到期打通）：拉 SPY 一条链 → raw_chain 落库 → 清洗 → IV → 平滑 → B-L → 分位数 → matplotlib 出一张"密度曲线 + 五根分位界桩"图。
2. 无套利检查 + 质量闸门。
3. 回填 1 年 × 5 标的，跑批重算。
4. 指标层 + 252 日滚动分位。
5. FastAPI 接口 + Vue3 仪表盘：密度曲线、K 线上的分位扇形带 + 冻结线覆盖、指标分位面板、多标的并排（个股 vs QQQ）、闸门置灰逻辑。
   - **信息架构：三层 + 底座**（2026-07-13 议定；全部视图层现算，不动指标集）：
     - *罗盘（持仓 × 冻结线）*：K 线 + 扇形带 + 冻结线；偏移量读数 (实时 Q − 冻结 Q)/σ1；失效判定灯（收盘确认制）；密度时序热力图（日期 × moneyness × 密度，叠现货路径）；双日分布对比器（默认"入场日 vs 今日"）。
     - *雷达（状态异动，受闸门约束）*：252 日分位面板；双峰事件灯（两峰位置 + 质量）；个股 vs QQQ 并排；ΔCDF 质量迁移曲线（日环比逐行权价）；尾部概率不对称时序（双线 + 比值柱）。
     - *温度计（环境与成本）*：IV 分位、期限结构双线 + 倒挂高亮；PIT 校准直方图（历史 CDF 代入到期实际结算价，量化 RN vs 真实的楔子，仅已到期分布可用）。
     - *底座（质量与运维，跨层）*：Smile 拟合诊断视图（IV 散点 + 样条 + 外推段 + 报价区阴影，标注 fit_meta 关键值，闸门置灰的申诉通道）；事件条带（roll 打点 / 闸门置灰 / 双峰亮灯 / 外推区虚线，所有时序图共享脚注）。
   - **可视化占位**（2026-07-13 议定：本期不做，留位后续，各自写明解锁条件）：
     - *3D 波动率曲面*——当前仅两个月度到期，切片不成面。解锁条件：到期覆盖扩展（周度或 ≥3 个月度入库）。
     - *σ 归一化分位轨道*（(Q−F)/σ1 时序）——与 Bowley skew 的 252 日分位信息高度重叠。解锁条件：实盘使用中出现 Bowley 分辨不了的具体形态场景。
     - *希腊字母面板*（自算口径）——EOD 无"实时"语义，展示型需求暂缺。解锁条件：付费升级日内数据（§1 升级触发条款生效）；届时希腊值一律自算，延续不信 vendor 的立场。
6. 动态槽准入检查器。

**Skeleton 阶段验收基准**：SPY 30DTE 的 ±1σ 与 ATM straddle 报价隐含的 expected move 量级一致；**F − Q50** 的符号与 25Δ RR（call − put 口径）的符号一致。两条都过，管线大概率是对的。

> 符号修正（2026-07-13，实现时发现）：原文为"Q50 − F"。左偏分布数学上必然中位数 > 均值（均值被左尾拉低），即 smirk 常态下 Q50 > F 且 RR(c−p) < 0——原表述在正常市场必挂。与 §4"RR–偏度同向"闸门（同用 c−p 口径，smirk 下两者皆负）核对后，正确的同向性检验是 sign(F − Q50) = sign(RR25)。
